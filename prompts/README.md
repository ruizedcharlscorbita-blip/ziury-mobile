# Reusable Prompts Directory

This folder is a placeholder repository to store complex, reusable prompt files, system instruction blocks, or custom task descriptions for AI assistants.

## Best Practices
- Organize prompts using lowercase kebab-case.
- Save files using the `.md` or `.txt` extension.
- Use explicit inputs and output expectations in prompt text.
